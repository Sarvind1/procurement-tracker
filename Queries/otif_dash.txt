SELECT
        rdata."id"::NUMERIC AS "internal id",
        rdata."date_created"::DATE AS "date created",
        rdata."document_number"::VARCHAR(50) AS "document number",
        rdata."scm_associated_brands"::VARCHAR(255) AS "associated brands",
        rdata."ID"::NUMERIC AS "id",  -- Corrected column name
        rdata."po_vendor"::VARCHAR(255) AS "name",
        rdata."supplier_confirmation_status"::VARCHAR(50) AS "supplier confirmation status",
        rdata."final_status"::VARCHAR(50) AS "final status",
        rdata."scm_po_scm_memo"::VARCHAR(255) AS "memo (main)",
        rdata."marketplace_header"::VARCHAR(8) AS "market place",
        rdata."supplier_payment_terms"::VARCHAR(125) AS "supplier payment terms",
        rdata."incoterms"::VARCHAR(12) AS "incoterm",
        rdata."line_id"::NUMERIC AS "line id",
        rdata."item"::VARCHAR(125) AS "item",
        rdata."asin"::VARCHAR(16) AS "asin number",
        rdata."quantity"::NUMERIC AS "quantity",
        rdata."quantity_fulfilled/received"::NUMERIC AS "quantity fulfilled/received",
        rdata."quantity_on_shipments"::NUMERIC AS "quantity on shipments",
        rdata."first_prd"::DATE AS "first prd",
        rdata."prd"::DATE AS "prd",
        rdata."planned_prd"::DATE AS "planned prd",
        rdata."confirmed_crd"::DATE AS "confirmed crd",
        rdata."quality_control_date"::DATE AS "quality control date",
        rdata."quality_control_status"::VARCHAR(255) AS "quality control status",
        rdata."im_line_signoff"::VARCHAR(8) AS "hs | sign-off shipment booking im line",
        rdata."sm_line_signoff"::VARCHAR(8) AS "hs | sign-off shipment booking sm line",
        rdata."production_status"::VARCHAR(125) AS "production status",
        rdata."batch_id"::VARCHAR(50) AS "batch_id",
        rdata."wh_type"::VARCHAR(12) AS "wh type",
        rdata."considered_for_anti-po"::VARCHAR(12) AS "considered for anti-po",
        rdata."prd_reconfirmation"::VARCHAR(12) AS "prd reconfirmed",
        rdata."invoice_number"::VARCHAR(50) AS "invoice number",
        rdata."invoice_status"::VARCHAR(125) AS "invoice status",
        rdata."Placement Batch"::VARCHAR(125) AS "summary filter",  -- Corrected column name
        rdata."OTIF Focus"::VARCHAR(12) AS "otif focus",
        rdata."MP"::VARCHAR(12) AS "mp",
        rdata."item_rate_eur"::FLOAT AS "per unit amount",  -- Corrected column name
        rdata."Pending Units"::NUMERIC AS "pending units",
        rdata."Pending Value"::FLOAT AS "pending value",
        rdata."po_razin_id"::VARCHAR(125) AS po_razin_id,
        rdata."Line Payment Type"::VARCHAR(8) AS "line payment type",
        rdata."Batch Payment Type"::VARCHAR(8) AS "batch payment type",
        rdata."INB Payment Type"::VARCHAR(8) AS "inb payment type",
        rdata."Line Invoice Submission Status"::VARCHAR(125) AS "line invoice submission status",
        rdata."Batch Invoice Submission Status"::VARCHAR(125) AS "batch invoice submission status",
        rdata."INB Invoice Submission Status"::VARCHAR(125) AS "inb invoice submission status",
        rdata."Line Payment Status"::VARCHAR(125) AS "line payment status",
        rdata."Batch Payment Status"::VARCHAR(125) AS "batch payment status",
        rdata."INB Payment Status"::VARCHAR(125) AS "inb payment status",
        rdata."Batch QC Pending"::VARCHAR(8) AS "batch qc pending",
        rdata."VP Booking Status"::VARCHAR(125) AS "vp booking status",
        rdata."FOB Date"::DATE AS "fob date",
        rdata."FOB Status"::VARCHAR(125) AS "fob status",
        rdata."Batch Pickup Status"::VARCHAR(125) AS "batch pickup status",
        rdata."Shipping Status"::VARCHAR(125) AS "shipping status",
        rdata."INB#"::VARCHAR(50) AS "inb#",
        rdata."Status"::VARCHAR(125) AS "shipment_status",  -- Corrected column name
        rdata."Substatus"::VARCHAR(125) AS "shipment_substatus", -- Corrected column name
        rdata."Estimated OTIF Delivery Date"::DATE AS "estimated otif delivery date",
        rdata."Supplier Telex Status"::VARCHAR(125) AS "supplier telex status",
        rdata."SM Telex Status"::VARCHAR(125) AS "sm telex status",
        rdata."FFW Telex Status"::VARCHAR(125) AS "ffw telex status",
        rdata."CM"::VARCHAR(125) AS "cm",
        rdata."SM"::VARCHAR(125) AS "sm",
        rdata."Compliance Status"::VARCHAR(125) AS "compliance status",
        rdata."Current Status"::VARCHAR(125) AS "current status",
        rdata."Sub Status"::VARCHAR(125) AS "sub status",
        rdata."Days Bucket"::VARCHAR(50) AS "days bucket",
        rdata."Team"::VARCHAR(12) AS "team",
        rdata."Reporting Status"::VARCHAR(125) AS "reporting status",
        rdata."Reporting Days Bucket"::VARCHAR(50) AS "reporting days bucket",
        rdata."L2 Final Status"::VARCHAR(50) AS "l2 final status",
        rdata."Final POC"::VARCHAR(125) AS "final poc",
        rdata."Final Team"::VARCHAR(50) AS "final team",
        rz_snapshot_ts AS snapshot_ts
    FROM
        razor_db.sharepoint_imports_prep.otif_base_data a
    QUALIFY Dense_RANK() OVER (ORDER BY rz_snapshot_ts DESC) = 1;